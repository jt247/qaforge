# Findings index
One row per finding. IDs are stable: `<PRODUCT-SLUG>-F-001`, `-F-002`, and so on, never reused. Status is one of open, confirmed, disputed, fixed-pending-retest, closed, rejected. Full detail lives in `findings/<ID>.md` using templates/finding.md.

| ID | Severity | Status | First run | Retest run | Title |
|---|---|---|---|---|---|
