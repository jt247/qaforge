# Fixture lifecycle (example)
Use synthetic information and the dedicated inbox. Each run and agent has a
distinct namespace. Record created account IDs and starting states privately.
Validate alias acceptance before bulk creation. Delete only records owned by
the run and only within approved scope; document cleanup or retained fixtures.
Account creation needs fixture-write in the environment scope.
